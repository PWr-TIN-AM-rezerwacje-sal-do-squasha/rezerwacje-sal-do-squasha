<%= form_with(model: court) do |form| %>
  <% if court.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(court.errors.count, "error") %> prohibited this court from being saved:</h2>

      <ul>
        <% court.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :courtNumber, style: "display: block" %>
    <%= form.text_field :courtNumber %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
